name: Deploy tema

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: FTP
    steps:
      - uses: actions/checkout@v4

      - name: Inspecionar estrutura
        run: |
          pwd
          ls -la
          find . -maxdepth 3 -type d -name "agenciaaids" || true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Minificar app.js
        run: |
          npm install -g terser
          terser assets/js/app.js -o assets/js/app.min.js -c -m

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps
          port: 21
          # 🔽 AJUSTE ESTA LINHA para o caminho REAL do tema no repo
          local-dir: ./
          # 🔽 Caminho remoto que você informou
          server-dir: /wp-content/themes/agenciaaids/
          timeout: 190000
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.github/**
            **/node_modules/**
            **/tests/**
            **/.vscode/**
            **/*.map
            **/README.md
